# Unified discovery (run_discovery.py with network + storage + remote servers)
# For agent-only (discovery_agent.py) no install needed (stdlib only).

netmiko>=4.0.0
PyYAML>=6.0
paramiko>=3.0.0
pywinrm>=0.4.0
